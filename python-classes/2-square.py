#!/usr/bin/python3
"""This module contains error checking"""


class Square:
    """This class includes error checking"""
    def __init__(self, size=0):
        if not isinstance(size, int):
            raise TypeError("size must be an integer")
        elif size < 0:
            raise ValueError("size must be >= 0")
        else:
            self.__size = size
